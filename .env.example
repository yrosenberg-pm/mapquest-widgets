# Copy to ".env.local" and fill in values. Do not commit .env.local.

# Mapillary (server-side only; never expose client secret in the browser)
MAPILLARY_CLIENT_ID=
MAPILLARY_CLIENT_SECRET=
MAPILLARY_AUTH_URL=
MAPILLARY_ACCESS_TOKEN=
